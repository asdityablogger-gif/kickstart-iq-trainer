import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "app.lovable.footballiq",
  appName: "Learn Basic Football IQ",
  webDir: "dist/client",
  android: {
    backgroundColor: "#0d1f1a",
  },
};

export default config;
